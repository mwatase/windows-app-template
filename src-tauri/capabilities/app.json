{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "app",
  "description": "What this program needs beyond the chassis. Widen it one permission at a time, deliberately: this software runs on machines belonging to other people. The entries below are the demo's and `npm run init` removes them.",
  "windows": ["main"],
  "permissions": [
    "dialog:allow-open",
    "notification:allow-is-permission-granted",
    "notification:allow-request-permission",
    "notification:allow-notify",
    {
      "identifier": "http:default",
      "allow": [{ "url": "https://api.github.com/repos/*" }]
    }
  ]
}
